import { ApiMeta } from '../../core/api/api.meta';

export const LOTS_META: ApiMeta = { endpoint: 'lots' };
export const AUTH_META: ApiMeta = { endpoint: 'auth' };
export const LOOKUPS_META: ApiMeta = { endpoint: 'lookups' }; // თუ დაამატებ lookups controller-ს
